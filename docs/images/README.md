# Screenshots for the main README

Capture these and save with the exact filenames below (the main README
references them). All are from the running stack.

1. **grafana-dashboard.png** — Grafana → CryptoSentinel dashboard, showing
   "Transactions by Chain" (both ETH + Polygon lines) + threat tiers.
   (localhost:3000)
2. **jaeger-trace.png** — Jaeger → a `cryptosentinel-api` trace expanded to
   show the span hierarchy. (localhost:16686)
3. **flamegraph.png** — open benchmark-data/profiles/analyze_transaction_flame.svg
   in a browser, screenshot it (or convert the SVG to PNG).
4. **dashboard-soc.png** — the Streamlit SOC dashboard main page.
   (localhost:8501)
