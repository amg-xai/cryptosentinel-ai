# TEMPLATE ONLY — never commit real values
# Real secrets managed by HashiCorp Vault in production
# Apply with: kubectl create secret generic cryptosentinel-secrets \
#   --from-literal=DB_PASSWORD=<value> \
#   --from-literal=GRAFANA_PASSWORD=<value> \
#   -n cryptosentinel
apiVersion: v1
kind: Secret
metadata:
  name: cryptosentinel-secrets
  namespace: cryptosentinel
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_WITH_VAULT_SECRET"
  GRAFANA_PASSWORD: "REPLACE_WITH_VAULT_SECRET"
  ETH_WS_URL: "REPLACE_WITH_VAULT_SECRET"
  ETH_HTTP_URL: "REPLACE_WITH_VAULT_SECRET"
